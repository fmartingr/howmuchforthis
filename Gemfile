source 'https://rubygems.org' do
  # Web framework plus web server
  gem 'sinatra', '~> 1.4'
  gem 'thin'

  # XML/XPath parsing
  gem 'nokogiri'

  group :development do
    gem 'sinatra-reloader'
  end
end
